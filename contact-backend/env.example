# Gmail credentials for sending emails
# You can use your domain email (contact@fdsegypt.com) or a Gmail account
GMAIL_USER=contact@fdsegypt.com
GMAIL_PASS=your-app-password

# Note: If using a domain email, you may need to configure SMTP settings
# or use a service like SendGrid, Mailgun, or AWS SES instead of Gmail

# Port (Railway will set this automatically)
PORT=5000
